version: '3.8'
services:
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: nestjs
      MYSQL_USER: dev
      MYSQL_PASSWORD: password
    ports:
      - 33206:3306
